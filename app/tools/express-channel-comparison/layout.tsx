import { createToolLayout, createToolMetadata } from '@/lib/tools/tool-seo'

export const metadata = createToolMetadata('express-channel-comparison')
export default createToolLayout('express-channel-comparison')
