import { createToolLayout, createToolMetadata } from '@/lib/tools/tool-seo'

export const metadata = createToolMetadata('delivery-date-calculator')
export default createToolLayout('delivery-date-calculator')
