import { createToolLayout, createToolMetadata } from '@/lib/tools/tool-seo'

export const metadata = createToolMetadata('pallet-load-calculator')
export default createToolLayout('pallet-load-calculator')
