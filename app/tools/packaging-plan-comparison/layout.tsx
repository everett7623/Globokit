import { createToolLayout, createToolMetadata } from '@/lib/tools/tool-seo'

export const metadata = createToolMetadata('packaging-plan-comparison')
export default createToolLayout('packaging-plan-comparison')
