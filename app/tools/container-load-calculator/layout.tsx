import { createToolLayout, createToolMetadata } from '@/lib/tools/tool-seo'

export const metadata = createToolMetadata('container-load-calculator')
export default createToolLayout('container-load-calculator')
